/*=k9,l0*/
//
int i, j, k, n;
void main() {
    i = 10;
    ++i;
    printf("inc %d\n", i);
    j = 5;
    --j;
    printf("dec %d\n", j);
    k = 0;
    n = 3;
    while (n > 0) {
        ++k;
        --n;
    };
    printf("loop %d %d\n", k, n);
}
