/*=s8,k9,l0*/
// NIL is no longer predefined: a program that expects it must declare its
// own null pointer constant (see 76_null_constant).
typedef struct {
    int val;
} node;
node *p;
void main() {
    p = NIL;
    writeln("UNREACHABLE");
}
