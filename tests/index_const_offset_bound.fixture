/*=k9,l0*/
typedef struct { int x, y; } pair;
int neg[1..8];
int pos[-2..5];
int leftc[-2..5];
int nested[1..8];
__packed int:8 packed[1..12];
pair wide[1..4];
int matrix[1..3][-2..1];
int i, j, total;
void main() {
    i = 2;
    j = 1;
    neg[i] = 10;
    total = neg[i];
    pos[i] = total;
    total = pos[i];
    leftc[i] += total;
    nested[i] += total;
    packed[i] = total;
    packed[i] += 1;
    wide[i].x = packed[i];
    total += wide[i].x;
    matrix[i][j] = total;
    matrix[i][j] += 1;
}
