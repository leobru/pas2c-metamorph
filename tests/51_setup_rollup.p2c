/*=s8,l0*/
int setup() assembler;
void rollup(int mark) assembler;
//
typedef int *intptr;
int mark;
intptr p, q;
void main() {
    mark = setup();
    p = malloc(1);
    *p = 111;
    rollup(mark);
    q = malloc(1);
    *q = 222;
    writeln(" after rollup p=", *p:0);
}
