/*=k9,l0*/
// The implicit standard files can also be named explicitly as write's
// leading argument; they are recognized by their 30-word size.
void putln() assembler;
void main() {
    write(_output_, "EXPLICIT ");
    write(_output_, 42:0);  putln();
}
