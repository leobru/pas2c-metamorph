/*=k9,l0*/
static int explicit = 5;
int implicit = 7;

int helper() {
    return explicit + implicit;
}

void main() {
    explicit += 1;
    implicit += 2;
    printf(" STATIC %d %d %d\n", explicit, implicit, helper());
}
