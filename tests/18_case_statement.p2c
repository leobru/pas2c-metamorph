/*=p-,t-,s8,y+,k9,l0*/
extern _output_;
//
var x: int;
{
    for (x = 0; x < 5; x = x + 1)
    switch (x) {
        1: writeln('One');
        2: writeln('Two');
        3: writeln('Three');
    default:
        writeln('Other');
    }
}
.
