/*=p-,t-,s8,y+,k9,l0*/
extern _output_;
//
var x: int;
{
    for (x = 0; x < 5; x = x + 1)
    switch (x) {
        case 1: writeln('One'); break;
        case 2: writeln('Two'); break;
        case 3: writeln('Three'); break;
    default:
        writeln('Other');
    }
}
.
