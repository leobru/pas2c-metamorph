/*=k9,l0*/

typedef struct {
    int lo, hi;
} pair;

__packed pair tab[4];
int i;
pair p;

void main() {
    tab[0].lo = 10;
    tab[0].hi = 20;
    tab[1].lo = 30;
    tab[1].hi = 40;
    i = 1;
    p = tab[i];
    writeln("lo=", p.lo:0, " hi=", p.hi:0);
}
