/*=p-,t-,s8,y+,k9,l0*/
extern _output_;
//
var s1, s2: __packed char[1..6];
{
    s1 = 'HELLO ';
    s2 = 'HELLO ';
    
    if (s1 == s2) {
        writeln(' Strings are equal');
    } else {
        writeln(' Strings are different');
    }
}
.
