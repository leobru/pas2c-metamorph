/*=s8,y+,l0*/
extern _output_;
int setup() assembler;
void rollup(int mark) assembler;
//
typedef int *intptr;
int mark;
intptr p, q;
{
    mark = setup();
    p = malloc(1);
    *p = 111;
    rollup(mark);
    q = malloc(1);
    *q = 222;
    writeln(' after rollup p=', *p:0);
}
.
